services:
  angular-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    image: retail-store-angular:dev
    container_name: angular-dev
    ports:
      - "4200:4200"
    volumes:
      - ./src:/app/src
      - /app/node_modules
