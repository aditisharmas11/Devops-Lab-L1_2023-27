apiVersion: v1
kind: Service
metadata:
  name: project8-order-service
spec:
  selector:
    app: project8-order-service
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: ClusterIP
