apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: microservices-demo
  labels:
    app: product-service
    app.kubernetes.io/part-of: shopsphere
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
    - port: 3000
      targetPort: 3000
      name: http
