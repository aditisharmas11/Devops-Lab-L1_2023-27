apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: microservices-demo
  labels:
    app: order-service
    app.kubernetes.io/part-of: shopsphere
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - port: 3000
      targetPort: 3000
      name: http
